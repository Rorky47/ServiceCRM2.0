# Railway NIXPACKS configuration - optimized for faster builds
[phases.setup]
nixPkgs = ["nodejs-20"]

[phases.install]
cmds = [
  "npm ci --prefer-offline --no-audit --no-fund"
]

[phases.build]
cmds = [
  "npm run build"
]

[variables]
NEXT_TELEMETRY_DISABLED = "1"
